import CalloutBox from "@/components/docs/CalloutBox";
import PackageTabs from "@/components/docs/PackageTabs";

<h2 id="installation">Instalacion</h2>

<PackageTabs packageName="react-rescuer" />

<h3 id="peer-deps">Peer deps</h3>

- `react` >= 18
- `react-dom` >= 18

<CalloutBox kind="tip" title="Gestores de paquetes">
  Los comandos por gestor salen del README de la libreria: npm / pnpm / yarn /
  bun.
</CalloutBox>
